[package]
name = "encoder"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
anyhow.workspace = true
tracing.workspace = true
tracing-subscriber.workspace = true
gstreamer.workspace = true
gstreamer-app.workspace = true
gstreamer-video.workspace = true
gstreamer-allocators.workspace = true
capture.workspace = true
# NVENC path: cudaupload does not accept DMA-BUF memory, so the fd is
# mapped and copied to system memory before it enters the pipeline. See
# the note in Encoder::push_frame.
memmap2.workspace = true

[[bin]]
name = "encode-probe"
path = "src/bin/encode_probe.rs"
